apiVersion: v1
kind: Service
metadata:
  name: todoapi-qa
  labels:
    app: todoapi
    env: qa
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: todoapi
    env: qa