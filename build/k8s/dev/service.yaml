apiVersion: v1
kind: Service
metadata:
  name: todoapi-dev
  labels:
    app: todoapi
    env: dev
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: todoapi
    env: dev