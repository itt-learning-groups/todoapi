apiVersion: v1
kind: Service
metadata:
  name: todoapi-prod
  labels:
    app: todoapi
    env: prod
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: todoapi
    env: prod